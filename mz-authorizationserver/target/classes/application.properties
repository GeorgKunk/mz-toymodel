# PostgreSQL connection
spring.datasource.url=jdbc:postgresql://postgres:5432/marktzeitung
spring.datasource.username=marktzeitung
spring.datasource.password=marktzeitung
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# Server port
server.port=8080

# JWT signing key (for demo, matches config)
marktzeitung.jwt.secret=01234567890123456789012345678901

