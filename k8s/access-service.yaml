apiVersion: apps/v1
kind: Deployment
metadata:
  name: mz-content-bouncer
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mz-content-bouncer
  template:
    metadata:
      labels:
        app: mz-content-bouncer
    spec:
      containers:
        - name: mz-content-bouncer
          image: mz-content-bouncer:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
---
apiVersion: v1
kind: Service
metadata:
  name: mz-content-bouncer
spec:
  type: NodePort
  ports:
    - port: 8000
      targetPort: 8000
      nodePort: 31006
  selector:
    app: mz-content-bouncer
